const models=require("../models")
const db=require("../dbb/db")
function home_vat(req,res){
    
}

function add_common_problem(req,res){
    title=req.body.title
    desc=req.body.desc
    animal_type=req.body.type
    pub=req.session.username
}
